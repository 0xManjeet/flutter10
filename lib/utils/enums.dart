enum ResponseTypes {
  QuestionDoesNotExists,
  Success,
  UnexpectedError,
}

enum ActionTypes { NewQuestion, ShowDialog, EndQuiz, SubmitAnswers }

enum AnswerTypes {
  MCQ,
  Phone,
  Date,
  Text,
  GenderSelection,
}
